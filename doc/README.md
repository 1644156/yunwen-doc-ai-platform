# Demo Documents

Put local demo PDF or Markdown files in this directory when running the import workflow.

Large source documents, zip archives, generated Markdown, and parsed output are intentionally excluded from the public repository. For a public demo, prefer one or two small desensitized documents.
